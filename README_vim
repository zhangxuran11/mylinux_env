标签:
:tabnew filename    #打开新的标签页
gt/gT              #标签页跳转快捷键
tabc                #关标签
:tabs           列出标签页和它们包含的窗口。
                当前窗口显示 ">"。
                修改过的缓冲区显示 "+"。

zf200G              #在光标和200行之间创建折叠

za 打开和关闭折叠       
zr/zm  一层一层地打开和关闭折叠     
zR/zM   分别打开和关闭所有的折叠

zj 将光标跳转到下一个折叠处   
zk 跳转到上一个折叠处

zd  删除光标下的折叠    
zD  删除光标下的折叠以及嵌套的折叠   
zE 删除所有折叠标签
#创建的折叠当退出vim之后就失效了

ctags --c++-kinds=+p --fields=+iaS --extra=+q -R *
